# T008 Test Results

Date: 2026-08-16
Environment: Darwin arm64, Node.js 24.15.0, pnpm 11.1.3, Docker client/server 29.4.0

## TDD

- RED: `pnpm vitest run tests/unit/scaffold.test.ts tests/unit/agent-skill.test.ts tests/unit/dsh-plugin.test.ts tests/integration/cli.test.ts` failed because `src/agent-skill.ts`, the scaffold implementation and the `init` CLI did not exist.
- Runtime observation RED: the real probe had no `skills` observation and could not prove native Skill discovery.
- Release-contract RED: v0.3.0 package, CLI, documentation, changelog and security assertions failed against the v0.2.1 identity.
- GREEN: the focused scaffold, Skill, native adapter, probe, CLI, package and documentation set passed 39 tests across 8 files; `pnpm typecheck` passed.

## Release Acceptance

- `pnpm validate`: passed 108 tests across 21 files, contract/action/release checks, typecheck, coverage and build.
- `pnpm test:e2e`: passed all 8 real DSH lifecycle fixtures in 204.53 seconds.
- `pnpm test:bundle-e2e`: passed real DSH bundle install, `dsh_test` execution and optional `dsh-testkit` Skill discovery in 37.68 seconds.
- `pnpm test:pack`: passed `dsh-testkit-0.3.0.tgz` install, exports, declarations, one-command scaffold, byte-identical Skill and runner image construction.
- `npm pack --dry-run --json`: passed; the tarball contains `.agents/skills/dsh-testkit/SKILL.md`, compiled scaffold/Skill modules and v0.3.0 metadata.
- `go run github.com/rhysd/actionlint/cmd/actionlint@v1.7.12 /tmp/dsh-testkit-v030-actionlint/.github/workflows/dsh-lifecycle.yml`: passed the workflow generated by `init`.
- `npx --yes actionlint`: npm could not determine an executable for that package name; the official pinned `rhysd/actionlint` binary above supplied the required lint.
- `npx --yes publint`: passed with publint 0.3.23.
- `pnpm audit --prod --registry https://registry.npmjs.org/`: no known vulnerabilities.
- `pnpm licenses list --prod --json`: runtime dependencies are MIT, ISC or Python-2.0 licensed and compatible with MIT distribution.

## Hosted Evidence

- Show & Tell: <https://github.com/deepseek-ai/deepseek-harness/discussions/2038>
- Implementation PR, protected checks, release identities, public consumer verification and upstream submissions: pending publication.
